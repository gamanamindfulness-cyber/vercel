// frontend/src/components/Background.jsx (Updated for Static Image)
import React from "react";

const Background = () => (
  // Replaced the complex <ul> structure with a simple div
  <div className="app-background-image">
    {/* The image will be applied via CSS to ensure full coverage and persistence */}
  </div>
);

export default Background;
